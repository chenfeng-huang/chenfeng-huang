name: Metrics
on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  github-metrics:
    runs-on: ubuntu-latest
    steps:
      # 1) Custom Introduction card
      - name: Introduction card
        uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.METRICS_TOKEN }}
          user: chenfeng-huang
          filename: metrics.plugin.introduction.svg
          base: ""                          # only plugins
          plugin_introduction: yes
          plugin_introduction_title: yes
          plugin_introduction_about: |
            Hi there, my name is Chenfeng Huang.
            I was born in Chengdu, China 🇨🇳 and now study Applied Data Science at USC.
            My current research focuses on Time Series OOD Generalization.

            I am also actively competing in Kaggle challenges at the Expert level
            🔗 [Kaggle Profile](https://www.kaggle.com/alrickh)

            Beyond data science, I am passionate about coffee brewing.
            Check out my coffee notes here:
            🔗 [Homepage](https://chenfenghuang.info)
          output_action: commit
          committer_branch: main
          committer_message: "chore: update introduction metrics"

      # 2) Classic metrics card (built-in About me)
      - name: Classic metrics
        uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.METRICS_TOKEN }}
          user: chenfeng-huang
          template: classic
          filename: github-metrics.svg
          base: header, activity, community, repositories, metadata
          config_timezone: America/Los_Angeles
          output_action: commit
          committer_branch: main
          committer_message: "chore: update classic metrics"
